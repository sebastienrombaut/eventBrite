<div align="center">

<h1>On crée un fat event là 🙂</h1><br>

<% if logged_in? %>
	<%= form_for @event, url: {action: "create"}, html: {class: "nifty_form"} do |f| %>
	  <%= label_tag 'Description de ton Event :' %>
	  <%= f.text_field :description %>
	  <%= label_tag 'Date :' %>
	  <%= f.date_field :date %>
	  <%= label_tag 'Lieu :' %>
	  <%= f.text_field :place %>
	  <%= f.submit "Crée ton fat event" %>

	  <% if @event.errors.any? %>
	    <div id="error_explanation">
	      <h2>
	        <%= pluralize(@event.errors.count, "error") %> prohibited
	        this event from being saved:
	      </h2>
	      <ul>
	        <% @event.errors.full_messages.each do |msg| %>
	          <li><%= msg %></li>
	        <% end %>
	      </ul>
	    </div>
	  <% end %>
<% end %>

<% else %>
	<p>Connecte toi pour créer un Event !!</p>
	<%= link_to "Login", login_path, class: "btn btn-info" %>



<% end %>
</div>